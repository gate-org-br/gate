package gate.adapter.metadata;

public class IDMetadata extends SimpleMetadata
{
	public IDMetadata() {super(builder().description("Campos de ID devem ser preenchidos com números inteiros positivos."));}
}