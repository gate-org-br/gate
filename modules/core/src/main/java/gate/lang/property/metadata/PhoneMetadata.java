package gate.lang.property.metadata;

public class PhoneMetadata extends SimpleMetadata
{
	public PhoneMetadata() {super(builder().description("Campos de número de telefone devem ser compostos apenas por dígitos"));}
}
