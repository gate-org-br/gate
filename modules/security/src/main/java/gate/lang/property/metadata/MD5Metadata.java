package gate.lang.property.metadata;

import gate.adapter.metadata.SimpleMetadata;

public class MD5Metadata extends SimpleMetadata
{
	public MD5Metadata() {super(builder().description("MD5 Hash"));}
}