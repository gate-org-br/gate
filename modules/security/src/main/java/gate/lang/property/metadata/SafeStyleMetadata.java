package gate.lang.property.metadata;

import gate.adapter.metadata.SimpleMetadata;

public class SafeStyleMetadata extends SimpleMetadata
{
	public SafeStyleMetadata() {super(builder().description("CSS inline seguro."));}
}