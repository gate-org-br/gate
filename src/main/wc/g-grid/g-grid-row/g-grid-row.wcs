* {
	box-sizing: border-box;
}

:host(*)
{
	height: 38px;
	cursor: inherit;
	display: table-row;
}

:host(.even),
:host(:nth-child(even))
{
	background-color: #FFFFFF;
}

:host(.odd),
:host(:nth-child(odd))
{
	background-color: #FAFAFA;
}

:host(:hover){
	background-color: var(--hovered);
}