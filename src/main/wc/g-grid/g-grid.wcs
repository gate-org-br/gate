* {
	box-sizing: border-box;
}

caption:empty,
thead:empty,
tbody:empty,
tfoot:empty
{
	display: none;
}

:host([empty]) > table {
	display: none;
}

:host(:not([empty])) > g-callout {
	display: none;
}


tbody > tr {
	cursor: pointer;
}

tbody > tr:hover > td{
	background-color: var(--hovered);
}