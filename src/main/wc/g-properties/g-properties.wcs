:host(*) {
	display: grid;
	grid-template-columns: auto auto;
}

::slotted(label) {
	padding: 4px;
	display: flex;
	font-weight: bold;
	align-items: center;
	justify-content: flex-end;
}

::slotted(span) {
	padding: 4px;
	display: flex;
	align-items: center;
	justify-content: flex-start;
}