dialog
{
	min-width: 320px;
	max-width: 600px;
	height: fit-content;
	width: calc(100% - 40px);
}

dialog > section {
	align-items: stretch;
}

g-date-time-interval-selector {
	flex-grow: 1;
}

dialog > footer > button {
	flex-grow: 1;
	justify-content: center;
}